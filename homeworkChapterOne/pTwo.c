// Take a number(n) from user & output its cube(n*n*n).
#include<stdio.h>

int main() {
    float n;
    printf("Please add a number: ");
    scanf("%f", &n);
    printf("The cube of the given number is %f", n*n*n);
    return 0;
}